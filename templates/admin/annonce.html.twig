{% extends 'base.admin.html.twig' %}

{% block body %}

<div class="container mt-4">

    <h1> Modifier l'annonce</h1>

    <form method="post">

        <div class="mb-3">
            <textarea name="info" class="form-control" rows="6">{{ annonce.info }}</textarea>
        </div>

        <button type="submit" class="btn btn-primary">
            Enregistrer
        </button>

    </form>

</div>

{% endblock %}